<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 从SDCard读取数据权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/treee"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/treee"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.TagSearchActivity"></activity>
        <activity android:name=".GameinfoActivity" />
        <activity android:name=".User.PasswordFindActivity" />
        <activity android:name=".User.PasswordChangeActivity" />
        <activity android:name=".User.UserLoginActivity" />
        <activity android:name=".User.UserInfoActivity" />
        <activity android:name=".Tools.DownloadActivity" />
        <activity android:name=".Tools.SearchActivity" />
        <activity android:name=".View.FocusActivity" />
        <activity android:name=".MyGameActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".Tools.SettingActivity" />
        <activity android:name=".CommentActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>